---
title: 여섯 앱 디자인 시스템
type: design-system
tags: [design, system, checklist]
updated: 2026-09-07
applies-to: [myjane, SnapWord, SnapNote, fitlog, 2hbk, typelog]
---

# 여섯 앱 디자인 시스템

**어느 앱을 만지든 이 노트를 먼저 읽는다.** [[결쩜사 디자인 시스템]]과 [[결쩜사 페이지 패턴]]은
*레퍼런스 사이트를 기술한* 노트다. 이 노트는 **우리 여섯 앱이 실제로 지키는 것**이다.
둘이 갈리는 지점을 아래 "결쩜사와 갈라진 곳"에 모았다.

## 가장 먼저 — 검사부터 돌린다

```bash
cd C:/Dev/myjane && npm run design:check
```

글로만 적어둔 규칙은 세션마다 놓친다. 실제로 그래서 이런 일이 있었다.

- myjane 만 원형 장식을 걷고 **다섯 앱에 그대로 남아** 인상이 갈렸다 (2026-09-07 해소)
- 금색을 글자로 쓴 자리가 5곳 (흰 시트 위 2.01:1)
- 강조색 배경에 순수 검정을 얹은 자리가 24곳 (3.01:1)
- 선언 없이 쓰이는 CSS 변수 19개 — 그 화면들은 카드 배경이 통째로 빠진 채 렌더됐다

검사기는 `myjane/scripts/design-check.mjs` 다. 규칙 A~G 와 **기준선**을 들고 있다.
기준선은 "아직 못 고쳤지만 늘어나면 실패"라는 뜻이다 — 0으로 만들 수 없다고
규칙을 지우면 다시 스며든다. `--all` 로 기준선 항목을 펼쳐 본다.

## 결쩜사와 갈라진 곳

| 결쩜사 | 우리 | 왜 |
|---|---|---|
| 실(結) 곡선 장식 (타원 호 + 금색 점) | **쓰지 않는다** | 밝은 바탕에서 얇은 선이 얼룩처럼 보인다 |
| 보라 `#7C3AED` | 먹청 `#116271` | 색까지 같아서 너무 닮았다 → [[먹청 톤 팔레트]] |
| 라이트 전용 | fitlog·2hbk 는 다크·커스텀도 | 사용자가 고를 수 있게 |

**가져온 것은 그대로 쓴다** — 시트 쌓기, `eyebrow → headline → lead` 순서,
짙은 시트로 히어로를 잡고 어두운 푸터로 닫기, 금색은 강조점에만,
헤드라인 명조(Gowun Batang 700), 순수 검정·회색을 쓰지 않는 원칙.

### 원형 장식 — 되살리지 말 것

2026-09-07 에 여섯 앱에서 걷었다. 걷어낸 것은 셋이다.

```
components/Sheet.tsx   Ornament 함수 · ornament prop · 타입 · 렌더 조각
app/globals.css        .sheet-ornament 규칙
호출부                  ornament 속성 (앱마다 1~12곳)
```

⚠️ **`<circle>` 이 들어간 SVG 를 일괄로 훑으면 안 된다.** 내용인 SVG 가 있다 —
FloatingChat·home 의 스마일리, fitlog `BodyRadar` 의 레이더 차트,
SnapWord `BouncingSmiley` 의 무지개색 배열.
장식만 정확히 집는다: `.sheet-ornament` 규칙과 `ornament` prop.

`radial-gradient(circle at …)` 는 **남긴다.** 이름에 circle 이 붙어 있어도
테두리 없는 확산 글로우이고, 짙은 시트의 빛이 이쪽이다.

prop 을 no-op 으로 남기지 말고 타입에서 없앤다 — 빠진 호출부를 `tsc --noEmit` 이 잡는다.

## 포인트 요소 넷 (2026-09-07 · myjane 에서 먼저)

결쩜사에서 가져왔다. **CSS 원본은 한 곳뿐이다** — 팔레트와 같은 구조다.

```
myjane/design/elements.css                       ← 여기만 고친다
cd C:/Dev/myjane && npm run elements -- --write   ← 여섯 앱이 함께 갱신된다
```

여섯 앱의 `app/elements.css` 는 **생성 파일**이다. `npm run elements` 는 쓰지
않고 검사만 하며, CSS 말고 앱마다 필요한 것들도 함께 본다 — `layout.tsx` 의
import 순서, `ScrollProgress.tsx` 존재, 다섯 앱 `Sheet.tsx` 동일성.

설계 근거와 측정값 → [[A 디자인 요소 추가]]

⚠️ **루트 페이지에만 넣지 않는다.** 껍데기에 해당하는 것(진행 띠)은 로그인·가입·
법적 페이지·앱 화면까지 **모든 페이지**에 있어야 한다. 루트만 다르게 생기면
같은 서비스로 보이지 않는다. 실제로 2026-09-07 에 루트만 손봤다가 지적받았다.
넣는 자리는 껍데기 컴포넌트다 —

```
myjane   components/AuthShell.tsx · components/LegalShell.tsx · app/page.tsx
다섯 앱  components/TopNav.tsx (앱 화면) · app/page.tsx (소개 페이지)
```

`/admin` 은 예외다 — 별도 화면이고 `admin.css` 를 쓴다.
typelog `app/r/[token]` 도 예외다 — **상단 메뉴를 일부러 두지 않은** 공유 링크 화면이다.

| 요소 | 클래스 | 어디에 |
|---|---|---|
| 스크롤 진행 띠 | `.scroll-progress` + `components/ScrollProgress.tsx` | **모든 페이지**의 헤더 안, 한 페이지에 하나 |
| 형광 밑줄 | `.mark` | 밝은 시트의 헤드라인, **한 화면에 한 군데** |
| 프로세스 타임라인 | `.flow` / `.flow-step` / `.flow-num` / `.flow-badge` | 단계가 3개 이상이고 **순서가 중요할 때만** |
| 라운딩 포인트 | `.sheet--point` | 아래 표 — **부모 시트에만** |

### 진행 띠 — 두 테마색 사이를 잇는다

색은 `--progress-from` → `--progress-to` 그라디언트다. **띠는 폭 100% 로 칠한 뒤
`scaleX` 로 줄이므로 그라디언트도 같이 눌린다** — 그래서 띠가 15% 든 95% 든
양쪽 끝 색이 늘 보이고 가운데만 부드럽게 섞인다.

고정 폭 그라디언트를 마스크로 드러내는 방식과 **결과가 다르다.** 그렇게 하면
초반에 왼쪽 색만 보인다. 결쩜사가 쓰는 방식이 앞의 것이다 (실측:
`position: fixed; top: 60px; height: 3px; z-index: 299`, `style="width: 45%"`,
`linear-gradient(90deg, #7c3aed, #c9a84c)`). width 를 %로 주는 것과 `scaleX` 는
같은 결과인데, `scaleX` 는 레이아웃을 다시 계산하지 않아 스크롤 중에 더 싸다.

| 실측 (myjane 라이트) | 왼쪽 끝 | 오른쪽 끝 |
|---|---|---|
| 15% · 35% · 60% · 85% · 100% 전부 | `#116271` 6.98:1 | `#a8842f` 3.50:1 |

⚠️ 오른쪽 끝에 `--gold`(`#c9a84c`)를 쓰면 안 된다 — 흰 헤더 위 **2.29:1** 로
비텍스트 기준 3.0 에 미달이다. 진행 위치를 알려주는 바로 그 끝이 가장 안 보이게
된다. `--progress-to`(`#a8842f`)는 같은 금색 계열에서 3.50 까지 내린 값이다.
→ [[먹청 톤 팔레트]]

#### 띠를 담는 헤더는 자리를 지켜야 한다

`.scroll-progress` 는 `absolute` 다. 헤더가 스크롤과 함께 올라가면 띠도 사라진다.

| 어디 | 상태 |
|---|---|
| myjane 랜딩 `.site-top` | `elements.css` 가 sticky 로 만든다 |
| myjane 로그인·가입·법적 `.auth-topbar` | 같다 |
| 다섯 앱 랜딩 `headerStyle` | 인라인으로 이미 sticky |
| 다섯 앱 앱화면 `.topnav` | 이미 `position: fixed` |

⚠️ 다섯 앱 앱화면에서는 `.topnav` 이 아니라 **`.topnav-bar` 안**에 넣는다.
`.topnav` 은 햄버거를 열면 메뉴만큼 키가 자라서, 그 아래 변에 붙이면 띠가
메뉴 밑으로 내려간다. `elements.css` 가 `.topnav-bar` 에 `position: relative` 를 준다.

### 라운딩 포인트 — **부모만 깎는다**

좌상·우하만 깎아 대각선 방향성을 만든다. 시트 반경의 1/4 이라 24px 시트에서는
6px 이다. **두 모서리만 덮어쓴다** — 나머지 둘은 요소가 원래 갖던 반경을 그대로
쓴다(`border-radius` 를 통째로 쓰면 반경이 다른 요소에는 못 쓴다).

붙이는 법: 다섯 앱 `<Sheet point>` · myjane `className="sheet sheet--point"`.

#### 안쪽 카드는 평범한 둥글기를 그대로 둔다

**깎는 것은 부모(시트)뿐이다.** 그래야 "이 시트는 안에 카드를 담고 있다" 가
모서리만 보고 읽힌다. 자식까지 깎으면 부모와 자식이 같은 신호를 내서
계층이 사라진다.

> 2026-09-07 에 내가 **반대로** 만들었다 — 자식 카드를 깎고 부모를 그냥 뒀다.
> 사용자가 바로잡았다. 이 절을 읽고 방향을 확인할 것.

#### 어디에 붙이나

세 자리다. 앞의 둘은 문서를 여닫고, 셋째가 구조 신호다.

| 붙인다 | 왜 |
|---|---|
| 히어로 시트 | 페이지의 시작 |
| 마무리 CTA 시트 | 페이지의 끝. 히어로와 짝을 이룬다 |
| **안에 카드를 담은 시트** | "여기부터 카드가 나온다" 는 구조 신호 |

| 실제로 붙은 카드 담은 시트 | |
|---|---|
| SnapWord · SnapNote · fitlog | `WHAT YOU GET` |
| 2hbk | `THREE MODES` |
| myjane | 카테고리 시트 4개(HEALTH · TYPE · STUDY · HABIT) · `ABOUT MYJANE` |
| typelog | 없다 — 안쪽 카드가 없는 앱이다 |

⚠️ **개수로 막지 않는다.** 구조 신호라서 해당되는 시트가 여섯 개면 여섯 개 다
붙는 게 맞다. 한때 검사 규칙 G 가 "한 화면에 2개" 로 막았는데, 규칙을 지킨
페이지가 실패해서 걷어냈다. 되살리지 말 것.

여전히 넣지 않는 곳: **입력 폼 카드.** 읽고 채우는 곳이라 시선을 끌 이유가 없다.

### ⚠️ 옮길 때 반드시 밟는 함정

**① `elements.css` 는 `layout.tsx` 에서 globals.css 다음 줄에 불러야 한다.**
`globals.css` 안에 `@import` 하면 안 된다 — `@import` 는 파일 맨 앞이라야 하니
규칙이 늘 먼저 들어가고, 특이도가 같은 `.sheet--point` 가
`.sheet { border-radius: 24px }` 에 **조용히** 진다. 에러가 없다.

**② `.mark` 는 `.headline .mark` 로 써야 한다.**
`.headline span` (0,1,1) 이 `.mark` (0,1,0) 를 이긴다. 그 규칙의
`background-clip: text` + `color: transparent` 때문에 금색 띠가 사라지고 글자가
그라디언트로 칠해진다 — **대비 1.00.** 눈으로는 그냥 강조 글자로 보여 놓치기 쉽다.
`-webkit-text-fill-color: currentColor` 로 되돌린다.

**③ 다크 테마에서 형광펜은 성립하지 않는다.**
`[data-theme="dark"]` 에서는 `.mark` 가 **금색 밑줄 3px** 로 바뀐다.
형광펜은 "밝은 띠 + 어두운 글자" 인데 다크에서는 글자가 밝아야 하니 띠도
어두워야 하고, 그러면 금색이 탁한 올리브로 보인다. 띠를 밝히면 글자가
3.4:1 로 떨어진다. 두 조건을 동시에 만족하는 밝기가 없다.

**④ `.flow` 가운데 정렬은 `.flow--center` 를 붙여야 한다.**
헤드라인이 가운데인 시트(myjane 랜딩)에서만 붙인다. 왼쪽 정렬 시트에 붙이면
타임라인이 제 헤드라인보다 안쪽으로 들어가 어긋난다.

**⑤ 인라인 `borderRadius` shorthand 는 클래스의 longhand 를 먹는다.**
다섯 앱의 카드 스타일은 인라인 객체다. 인라인 shorthand 는 우선순위가 가장
높고 네 모서리를 모두 세우므로 클래스가 세운 두 모서리를 **조용히** 덮어쓴다.
지금은 인라인 스타일을 쓰는 카드에 `.sheet--point` 를 붙이지 않으니 부딪히지
않지만, 언젠가 붙일 때를 위해 적어 둔다.

**⑥ 토큰 어휘가 둘이다.**
myjane 은 `--text`/`--accent-soft`/`--text-dim`, 다섯 앱은
`--text-primary`/`--accent-subtle`/`--text-secondary`. `elements.css` 는 파일
머리에서 `--el-*` 로 다리를 놓아 한 파일로 여섯 앱을 돈다.
⚠️ 그 다리(`var(--x, 대체값)`)는 **규칙 B 가 못 본다** — 오타가 조용히 지나간다.

## 루트(소개) 페이지 헤더·푸터

다섯 앱이 같은 골격을 쓴다. 기준은 **fitlog** 다.

```tsx
<div style={{ minHeight: "100vh", background: "var(--bg-primary)" }}>
  <header style={headerStyle}>            // sticky · top 0 · z 40 · borderBottom
    <div className="page" style={{ ...headerInner, paddingTop: 14, paddingBottom: 14 }}>
      <span className="row" style={{ gap: 9 }}>
        <AppIcon size={30} priority />
        <span style={{ fontWeight: 900, letterSpacing: "-0.02em" }}>앱이름</span>
      </span>
      <LandingHeaderAuth />
    </div>
  </header>
  <main className="page">…</main>
  <footer style={footerStyle}>            // --footer-bg · --on-dark
    앱이름 · myjane 워드마크 · @2026 myjane All rights reserved
  </footer>
</div>
```

- 아이콘은 **`AppIcon` 컴포넌트**를 쓴다. 생 `<img src="/site-title-icon.png">` 를 쓰지 말 것
  (2hbk 가 그랬다). 컴포넌트는 기본 `alt` 만 앱마다 다르다
- 푸터 색은 토큰이다 — `--footer-bg` · `--on-dark` · `--on-dark-faint`.
  다섯 앱이 `#061a1f` · `#fff` · `rgba(230,244,246,0.62)` 를 박아 쓰고 있었다
- `LandingHeaderAuth` 는 로그인 상태에 따라 "내 기록" / "로그인" 으로 갈린다.
  상태가 정해지기 전에는 자리만 잡는다 → [[인증과 세션 공유]]
- TypeLog 은 `LandingCta` 가 없다. **로그인 없이도 해볼 수 있어서** 본문 CTA 가
  "타입 찾아보기"이고 세션에 따라 갈리지 않는다

### ⚠️ 폰트는 링크가 없으면 조용히 떨어진다

2026-09-07 에 사용자가 "글자체가 약간 달라 보인다"고 했다. 원인이 이것이었다.

  SnapWord · SnapNote   body 스택에 Pretendard 가 없음 → 시스템 기본
                        layout.tsx 에 폰트 링크 0건 → Gowun Batang 이 일반 명조로

`.headline` 은 다섯 앱 모두 `"Gowun Batang", serif` 를 요구한다.
**링크가 없어도 오류나 경고가 나지 않는다** — serif 로 조용히 대체된다.

`app/layout.tsx` 의 `<head>` 에 네 줄이 있어야 한다.

```
preconnect fonts.googleapis.com
preconnect fonts.gstatic.com (crossOrigin)
Gowun+Batang:wght@400;700
pretendard-dynamic-subset.min.css (jsdelivr)
```

body 스택: `Pretendard, -apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans KR", Roboto, sans-serif`

새 앱을 만들 때 이 네 줄을 빼먹으면 **다른 앱과 미묘하게 달라 보인다.**

## 색 — 원본은 한 곳이다

```
myjane/design/palette.json        ← 여기만 고친다
myjane 에서 npm run palette -- --write
→ 여섯 앱의 app/palette.css 가 다시 만들어진다 (생성 파일)
```

각 앱 `globals.css` 는 `@import "./palette.css"` 한 줄만 두고 색 선언이 없다.
쓰기 전에 대비 31건을 검사하고 **하나라도 미달이면 아무 파일도 쓰지 않는다.**
자세한 것은 → [[먹청 톤 팔레트]]

### 면적용과 글자용은 다른 토큰이다

이 팔레트에서 같은 문제를 **세 번** 겪었다.

| 면적 | 글자 | 왜 |
|---|---|---|
| `--accent` | `--accent-ink` | 살구를 썼을 때 2.37:1 |
| `--point` `--gold` | `--point-ink` | 금색 알약 위 금색 글자 2.01:1 |
| `--danger` `--success` | `--danger-ink` `--success-ink` | 흰 카드 위 4.05 / 3.35 |

먹청은 어두워서 `--accent` = `--accent-ink` 지만 **토큰은 따로 둔다.**
다음에 밝은 색을 고르면 그때 값만 갈라진다.

### 리터럴로 쓰지 않는 면들

지난 두 번의 색 교체에서 손이 가장 많이 간 자리다. 토큰이 있다.

```
--sheet-dark  --sheet-dark-glow  --footer-bg  --btn-gradient  --btn-shadow
--on-dark  --on-dark-dim  --on-dark-faint  --border-on-dark
--accent-on-dark  --gold-on-dark  --shadow-rgb
```

`--shadow-rgb` 는 `rgba(var(--shadow-rgb), 0.1)` 처럼 쓴다.

## 공통 컴포넌트

`components/Sheet.tsx` 는 다섯 앱에 **같은 파일이 복사돼 있다** (myjane 은 순수 마크업).

```tsx
<Sheet tone="plain|tint|dark|gold" eyebrow="STUDY · 공부 기록"
       headline={…} lead={…} center>
```

⚠️ 복사본이라 **한 앱만 고치면 갈라진다.** Sheet 를 바꿀 때는 다섯 앱을 함께 바꾸고,
바꾼 내용을 이 노트에 적는다. 색과 달리 컴포넌트는 아직 생성 장치가 없다 —
아래 "다음에 만들 것" 참고.

시트 배경은 **번갈아** 쓴다(`.sheet` / `.sheet--tint`). 순서를 바꿀 때
`sheet--tint` 가 연속되지 않는지 확인한다.

## 아이콘

여섯 개가 같은 기하학(짙은 타일 + 게이지 호)을 공유한다.
**하나만 바꾸면 가족이 어긋난다** — 항상 여섯 개를 함께 바꾸고, PNG 파생물 56개를
다시 만든다 → [[앱 공통 UI와 아이콘]]

## 검사 규칙 A~G

| | 규칙 | 왜 |
|---|---|---|
| A | 원형 장식 잔재 (`sheet-ornament` · `ornament`) | 위 참고 |
| B | 선언 없이 쓰이는 CSS 변수 | 선언이 없으면 그 속성이 통째로 빠진 채 렌더된다 |
| C | 면적용 색을 글자로 (`color: var(--point)` 등) | `-ink` 토큰을 쓴다 |
| D | 순수 검정·흰색을 글자색으로 | `--on-accent` (강조색 위) · `--on-dark` (짙은 면 위) |
| E | 토큰을 리터럴로 선언 (`globals.css` 안의 hex) | 원본은 `palette.json` 하나 |
| F | 폰트 링크 누락 | 링크가 없으면 **조용히** 일반 명조·시스템 폰트로 떨어진다 |
| G | 형광 밑줄 남발 (`mark` 2개 이상) | 헤드라인마다 칠하면 강조가 사라진다. `sheet--point` 는 **세지 않는다** — 구조 신호다 |

### 현재 기준선 (2026-09-07)

- **C 45건** — `--danger` 를 오류 문구에 쓰는 자리. `--danger-ink` 로 바꾸면 색이 조금
  어두워지고(`#e0455f → #a83447`), `--success` 는 색상까지 130°로 밀어야 해서
  개선 표시의 외형이 바뀐다. **사용자 확인이 필요하다.**
- **D 14건** — `#fff` 이 세 종류로 섞여 있다: 강조색 배경 위, 짙은 면 위,
  그리고 푸터처럼 배경까지 리터럴인 자리. 자리마다 달라 일괄 치환이 안 된다.
  (랜딩 푸터를 토큰으로 바꿔 18 → 14 로 줄었다)
- C 는 46 → 45 로 줄었다 — 주석을 검사에서 뺐고, 다섯 앱 타임라인 번호가
  `var(--accent)` 글자색을 쓰던 자리를 `.flow-num` 이 대체했다

규칙 B·C 는 **주석을 지운 사본**을 본다. 설명에 적은 `var(--radius-lg)` 를
실제 사용으로 읽어 오탐 7건을 낸 적이 있다.

A · B · E · F · G 는 0건이다. **이 다섯이 걸리면 새로 생긴 것이니 바로 고친다.**

⚠️ G 는 **파일당** 센다. 화면당이 아니다 — 마무리 CTA 는 `components/LandingAuth.tsx` 에
따로 있어서 랜딩 한 화면의 라운딩 2개가 두 파일에 나뉜다. 컴포넌트를 쪼개면
검사기를 속일 수 있으니, 숫자가 아니라 아래 표를 기준으로 판단한다.

## 예외로 빼둔 자리

검사기의 `ALLOW` 에 이유와 함께 적혀 있다. 새로 추가할 때는 **왜 예외인지** 함께 쓴다.

- `BouncingSmiley.tsx` · 단어 상세 — 튀는 스마일리용 무지개색 배열
- `ThemeProvider.tsx` — 커스텀 테마 기본값(사용자가 고르는 값)
- `api/auth/*` — 이메일 HTML. **CSS 변수를 못 쓴다.** 리터럴이 남을 수밖에 없는 유일한 자리
- `opengraph-image` — 런타임 CSS 가 없다
- `palette.css` — 생성 파일
- `admin.css` — 포털 admin. 팔레트 별칭을 쓰는 별도 화면

## 다음에 만들 것

- **`Sheet.tsx` · `ScrollProgress.tsx` 도 생성 장치로.** 색(`palette.json`)과
  포인트 요소 CSS(`design/elements.css`)는 원본이 하나가 됐지만 **컴포넌트는
  아직 복사본**이다. `npm run elements` 가 다섯 벌이 같은지 **검사만** 한다 —
  갈라지면 알려주지만 고쳐주지는 않는다.
- **`design:check` 를 배포 전에 자동으로.** 지금은 사람이 돌려야 한다.

## 볼 곳

- 색과 대비 → [[먹청 톤 팔레트]]
- 아이콘·워드마크 → [[앱 공통 UI와 아이콘]]
- 레퍼런스 원본 → [[결쩜사 디자인 시스템]] · [[결쩜사 페이지 패턴]]
- 문장 쓰는 법 → [[결쩜사 카피 톤앤매너]] · [[서비스 카테고리와 카피 원칙]]
- 검증 환경 → [[개발 서버와 검증 환경]]
